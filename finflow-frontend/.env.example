# Backend API base URL (no trailing slash). For local dev, run backend and use:
VITE_API_BASE_URL=http://localhost:8080

# Production (Option B: separate frontend/backend origins)
# VITE_API_BASE_URL=https://api.finflow.example
